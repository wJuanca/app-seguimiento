<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de mi Equipo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
    <style>
        .equipo-header {
          background-color: white;
          border-radius: 0.5rem;
          box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
          padding: 1.5rem;
          margin-bottom: 2rem;
          position: relative;
        }
        
        .equipo-icon {
          font-size: 3rem;
          color: #2563eb;
          margin-right: 1.5rem;
        }
        
        .estado-badge {
          position: absolute;
          top: 1.5rem;
          right: 1.5rem;
          padding: 0.25rem 0.75rem;
          border-radius: 9999px;
          font-size: 0.875rem;
          font-weight: 500;
        }
        
        .estado-disponible {
          background-color: #d1fae5;
          color: #065f46;
        }
        
        .estado-asignado {
          background-color: #dbeafe;
          color: #1e40af;
        }
        
        .estado-reparacion {
          background-color: #fef3c7;
          color: #92400e;
        }
        
        .estado-baja {
          background-color: #fee2e2;
          color: #b91c1c;
        }
        
        .equipo-details {
          background-color: white;
          border-radius: 0.5rem;
          box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
          padding: 1.5rem;
          margin-bottom: 2rem;
        }
        
        .detail-item {
          padding: 0.75rem 0;
          border-bottom: 1px solid #e5e7eb;
          display: flex;
        }
        
        .detail-item:last-child {
          border-bottom: none;
        }
        
        .detail-label {
          font-weight: 500;
          color: #4b5563;
          width: 150px;
        }
        
        .detail-value {
          flex: 1;
        }
        
        .timeline {
          position: relative;
          padding-left: 2rem;
        }
        
        .timeline::before {
          content: '';
          position: absolute;
          left: 0;
          top: 0;
          height: 100%;
          width: 2px;
          background-color: #e5e7eb;
        }
        
        .timeline-item {
          position: relative;
          padding-bottom: 1.5rem;
        }
        
        .timeline-item:last-child {
          padding-bottom: 0;
        }
        
        .timeline-dot {
          position: absolute;
          left: -2rem;
          top: 0;
          width: 1rem;
          height: 1rem;
          border-radius: 50%;
          background-color: #2563eb;
          border: 3px solid white;
        }
        
        .timeline-content {
          background-color: #f8fafc;
          border-radius: 0.5rem;
          padding: 1rem;
        }
        
        .timeline-date {
          font-size: 0.875rem;
          color: #64748b;
          margin-bottom: 0.5rem;
        }
        
        .timeline-title {
          font-weight: 600;
          margin-bottom: 0.5rem;
        }
        
        .timeline-description {
          color: #4b5563;
        }
        
        .back-btn {
          display: inline-flex;
          align-items: center;
          color: #2563eb;
          text-decoration: none;
          font-weight: 500;
        }
        
        .back-btn:hover {
          text-decoration: underline;
        }
        
        .back-btn i {
          margin-right: 0.5rem;
        }
      </style>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <div class="container mt-5">
        <a href="/mi-equipo" class="back-btn mb-4">
          <i class="bi bi-arrow-left"></i> Volver a mis equipos
        </a>
        
        <div class="equipo-header d-flex align-items-center">
          <i class="bi <%= 
            equipo.tipo === 'laptop' ? 'bi-laptop' : 
            equipo.tipo === 'desktop' ? 'bi-pc-display' : 
            equipo.tipo === 'impresora' ? 'bi-printer' : 
            equipo.tipo === 'servidor' ? 'bi-server' : 
            'bi-device-hdd' 
          %> equipo-icon"></i>
          
          <div>
            <h1 class="mb-1"><%= equipo.nombre %></h1>
            <p class="text-muted mb-0"><%= equipo.marca %> <%= equipo.modelo %></p>
          </div>
          
          <span class="estado-badge <%= 
            equipo.estado === 'disponible' ? 'estado-disponible' : 
            equipo.estado === 'asignado' ? 'estado-asignado' : 
            equipo.estado === 'en_reparacion' ? 'estado-reparacion' : 
            'estado-baja' 
          %>">
            <%= equipo.estado %>
          </span>
        </div>
        
        <div class="row">
          <div class="col-md-6">
            <div class="equipo-details">
              <h3 class="mb-3">Detalles del Equipo</h3>
              
              <div class="detail-item">
                <div class="detail-label">Tipo:</div>
                <div class="detail-value"><%= equipo.tipo %></div>
              </div>
              
              <div class="detail-item">
                <div class="detail-label">Marca:</div>
                <div class="detail-value"><%= equipo.marca %></div>
              </div>
              
              <div class="detail-item">
                <div class="detail-label">Modelo:</div>
                <div class="detail-value"><%= equipo.modelo %></div>
              </div>
              
              <div class="detail-item">
                <div class="detail-label">Número de Serie:</div>
                <div class="detail-value"><%= equipo.numero_serie %></div>
              </div>
              
              <div class="detail-item">
                <div class="detail-label">Estado:</div>
                <div class="detail-value"><%= equipo.estado %></div>
              </div>
            </div>
          </div>
          
          <div class="col-md-6">
            <div class="equipo-details">
              <h3 class="mb-3">Historial de Reparaciones</h3>
              
              <% if (reparaciones && reparaciones.length > 0) { %>
                <div class="timeline">
                  <% reparaciones.forEach(reparacion => { %>
                    <div class="timeline-item">
                      <div class="timeline-dot"></div>
                      <div class="timeline-content">
                        <div class="timeline-date">
                          <%= new Date(reparacion.fecha_inicio).toLocaleDateString() %>
                          <% if (reparacion.fecha_fin) { %>
                            - <%= new Date(reparacion.fecha_fin).toLocaleDateString() %>
                          <% } %>
                        </div>
                        <div class="timeline-title">
                          <%= reparacion.estado === 'pendiente' ? 'Pendiente' : 
                             reparacion.estado === 'en_proceso' ? 'En Proceso' : 
                             reparacion.estado === 'completada' ? 'Completada' : 
                             'Cancelada' %>
                        </div>
                        <div class="timeline-description">
                          <%= reparacion.descripcion %>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                </div>
              <% } else { %>
                <p class="text-muted">Este equipo no tiene historial de reparaciones.</p>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>