<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mis Equipos - Sistema de Gestión</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('../partials/header') %>

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-8">
            <h1>Mis Equipos Asignados</h1>
        </div>
        <div class="col-md-4 text-end">
            <a href="/mi-equipo/historial" class="btn btn-info">
                <i class="bi bi-clock-history"></i> Ver Historial Completo
            </a>
        </div>
    </div>

    
    
    <% if (mensajes && mensajes.success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>
        <%= mensajes.success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
      </div>
    <% } %>
    
    <% if (mensajes && mensajes.error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        <%= mensajes.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
      </div>
    <% } %>
    
    <div class="row g-4">
      <% if (equipos && equipos.length > 0) { %>
        <% equipos.forEach(equipo => { %>
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm border-0 hover-shadow">
              <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><%= equipo.nombre %></h5>
                <span class="badge bg-primary">
                  <i class="bi bi-laptop me-1"></i>
                  <%= equipo.tipo %>
                </span>
              </div>
              <div class="card-body">
                <div class="mb-3">
                  <div class="d-flex align-items-center mb-2">
                    <i class="bi bi-tag-fill text-primary me-2"></i>
                    <span class="fw-medium">Marca:</span>
                    <span class="ms-2"><%= equipo.marca %></span>
                  </div>
                  <div class="d-flex align-items-center mb-2">
                    <i class="bi bi-info-circle text-primary me-2"></i>
                    <span class="fw-medium">Modelo:</span>
                    <span class="ms-2"><%= equipo.modelo %></span>
                  </div>
                  <div class="d-flex align-items-center mb-2">
                    <i class="bi bi-upc-scan text-primary me-2"></i>
                    <span class="fw-medium">Serie:</span>
                    <span class="ms-2"><%= equipo.numero_serie %></span>
                  </div>
                  <div class="d-flex align-items-center">
                    <i class="bi bi-calendar-date text-primary me-2"></i>
                    <span class="fw-medium">Fecha Asignación:</span>
                    <span class="ms-2"><%= new Date(equipo.fecha_asignacion).toLocaleDateString() %></span>
                  </div>
                </div>
                
                <div class="alert alert-light border mb-0">
                  <div class="d-flex align-items-center">
                    <i class="bi bi-chat-left-text text-primary me-2"></i>
                    <span class="fw-medium">Razón de asignación:</span>
                  </div>
                  <p class="mb-0 mt-1 ps-4"><%= equipo.razon_asignacion || 'No especificada' %></p>
                </div>
              </div>
              <div class="card-footer bg-white border-top-0">
                <a href="/mi-equipo/detalles/<%= equipo.id_equipo %>" class="btn btn-primary w-100">
                  <i class="bi bi-eye me-2"></i>Ver Detalles
                </a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12">
          <div class="alert alert-info text-center py-5">
            <i class="bi bi-info-circle fs-1 d-block mb-3"></i>
            <h5>No tienes equipos asignados</h5>
            <p class="mb-0">Cuando se te asigne un equipo, aparecerá en esta sección.</p>
          </div>
        </div>
      <% } %>
    </div>
  
</div>

  <!-- Bootstrap 5 JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <style>
    .hover-shadow {
      transition: all 0.3s ease;
    }
    
    .hover-shadow:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
    }
    
    .fw-medium {
      font-weight: 500;
    }
  </style>
</body>
</html>

